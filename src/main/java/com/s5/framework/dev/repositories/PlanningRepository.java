package com.s5.framework.dev.repositories;

/**
 * La planification est désormais purement simulée (non persistée).
 * Ce fichier est conservé pour compatibilité de structure mais n'est plus utilisé.
 */
public interface PlanningRepository {
    // Vide — plus de persistance de planning en base.
}
