package com.s5.framework.dev.controllers;

/**
 * L'API REST de planning est désactivée — la planification est désormais une simulation
 * pure accessible via GET /planification.
 * Ce fichier est conservé pour compatibilité de structure.
 */
public class PlanningRestController {
    // Vide — plus d'API REST planning.
}
